import React from 'react'

const Layout = ({ children }) => {
    return (
        <div>
            {/* Header */}
            {/* Sidebar */}
            {children}
        </div>
    )
}

export default Layout
